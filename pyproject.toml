[project]
name = "polish-research-agent"
version = "0.1.0"
description = "Independent final pre-submit polish-and-report layer for Researka artifacts"
requires-python = ">=3.11"

[tool.pytest.ini_options]
testpaths = ["tests"]
